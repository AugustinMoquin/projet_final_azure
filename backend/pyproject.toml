[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]  # line length is enforced loosely for a student project

[tool.pytest.ini_options]
testpaths = ["tests"]
